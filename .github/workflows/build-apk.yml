name: Build 共看 APK on: workflow_dispatch: push: branches: [ main ] jobs: build: runs-on: ubuntu-latest steps: - uses: actions/checkout@v4 - uses: actions/setup-java@v4 with: distribution: temurin java-version: '17' - uses: android-actions/setup-android@v3 - uses: gradle/actions/setup-gradle@v4 with: gradle-version: '8.9' - name: Unpack 共看 source run: | rm -rf build-src mkdir build-src unzip -q gongkan-source.zip -d build-src  - name: Build debug APK working-directory: build-src run: gradle :app:assembleDebug - uses: actions/upload-artifact@v4 with: name: 共看-apk path: build-src/app/build/outputs/apk/debug/app-debug.apk
